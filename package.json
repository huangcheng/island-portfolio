{
  "name": "island-portfolio",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test:e2e": "node scripts/verify.mjs",
    "verify": "tsc --noEmit && pnpm build:chengim && node scripts/verify.mjs chengim",
    "verify:islands": "pnpm build:chengim && node scripts/verify.mjs chengim && pnpm build:chengsh && node scripts/verify.mjs chengsh && pnpm build:misthois && node scripts/verify.mjs misthois && pnpm build:kleos && node scripts/verify.mjs kleos && pnpm build:noveo && node scripts/verify.mjs noveo",
    "build:chengim": "vite build --mode chengim --outDir dist/chengim --emptyOutDir",
    "build:chengsh": "vite build --mode chengsh --outDir dist/chengsh --emptyOutDir",
    "build:misthois": "vite build --mode misthois --outDir dist/misthois --emptyOutDir",
    "build:kleos": "vite build --mode kleos --outDir dist/kleos --emptyOutDir",
    "build:noveo": "vite build --mode noveo --outDir dist/noveo --emptyOutDir",
    "islands": "pnpm build:chengim && pnpm build:chengsh && pnpm build:misthois && pnpm build:kleos && pnpm build:noveo"
  },
  "dependencies": {
    "@tanstack/react-router": "^1.170.22",
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "three": "^0.185.1",
    "troika-three-text": "^0.52.5"
  },
  "devDependencies": {
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.4",
    "@types/three": "^0.185.4",
    "@vitejs/plugin-react": "^6.0.5",
    "playwright": "^1.62.1",
    "typescript": "~5.9.3",
    "vite": "^8.2.1"
  }
}
